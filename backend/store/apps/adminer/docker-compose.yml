version: '3.8'
services:
  app:
    image: adminer:latest
    container_name: panel-adminer
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${ADMINER_PORT}:8080'
