version: '3.8'
services:
  app:
    image: 'xhofe/alist:latest'
    container_name: panel-alist
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${ALIST_PORT}:5244'
    volumes:
      - /www/server/apps/alist/data:/opt/alist/data
