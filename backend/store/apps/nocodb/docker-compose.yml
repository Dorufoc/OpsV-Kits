version: '3.8'

services:
  app:
    image: nocodb/nocodb:latest
    container_name: panel-nocodb
    restart: unless-stopped
    ports:
      - "${NOCODB_PORT}:8080"
    volumes:
      - /www/server/apps/nocodb/data:/usr/app/data
    labels:
      - managed-by=opsv-kits-store