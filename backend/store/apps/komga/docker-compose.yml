version: '3.8'
services:
  app:
    image: 'ghcr.io/gotson/komga:latest'
    container_name: panel-komga
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${KOMGA_PORT}:8080'
    volumes:
      - /www/server/apps/komga/data:/config
