version: '3.8'
services:
  app:
    image: 'ollama/ollama:latest'
    container_name: panel-ollama
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${OLLAMA_PORT}:11434'
    volumes:
      - /www/server/apps/ollama/data:/root/.ollama
