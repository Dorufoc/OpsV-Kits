version: '3.8'
services:
  app:
    image: 'linuxserver/plex:latest'
    container_name: panel-plex
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${PLEX_PORT}:32400'
    volumes:
      - /www/server/apps/plex/data:/config
