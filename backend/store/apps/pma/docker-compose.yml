version: '3.8'
services:
  app:
    image: phpmyadmin/phpmyadmin:latest
    container_name: panel-pma
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${PMA_PORT}:80'
    environment:
      - PMA_HOST=host.docker.internal
