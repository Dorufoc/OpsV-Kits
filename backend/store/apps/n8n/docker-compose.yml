version: '3.8'
services:
  app:
    image: 'docker.n8n.io/n8nio/n8n:latest'
    container_name: panel-n8n
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${N8N_PORT}:5678'
    volumes:
      - /www/server/apps/n8n/data:/home/node/.n8n
