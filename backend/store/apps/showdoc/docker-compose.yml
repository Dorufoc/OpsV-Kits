version: '3.8'
services:
  app:
    image: 'star7th/showdoc:latest'
    container_name: panel-showdoc
    restart: unless-stopped
    labels:
      managed-by: "opsv-kits-store"
    ports:
      - '${SHOWDOC_PORT}:80'
    volumes:
      - /www/server/apps/showdoc/data:/var/www/html
